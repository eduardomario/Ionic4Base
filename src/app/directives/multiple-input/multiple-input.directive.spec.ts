import { MultipleInputDirective } from './multiple-input.directive';

describe('MultipleInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MultipleInputDirective();
    expect(directive).toBeTruthy();
  });
});
